# Revenant Mobile Companion

Android and iOS cannot provide a universal raw deleted-file recovery API. Modern Android scoped storage and iOS sandboxing prevent arbitrary sector-level access without device-specific acquisition paths. This companion is therefore an acquisition/transfer client: it can hand accessible user-selected files/media to Revenant Core and display recovery sessions. It does not falsely claim raw filesystem recovery on stock devices.
