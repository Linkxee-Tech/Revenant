# Recovery test fixtures

Each fixture should define expected files and SHA-256 values. Required scenarios: deleted files, nested directories, fragmentation, large files, partial/corrupt files, chunk-boundary signatures, duplicates, overlapping candidates, bad sectors and multiple partitions.
